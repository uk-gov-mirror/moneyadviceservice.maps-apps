name: CI

trigger: none

pr:
  branches:
    include:
      - main
      - feature/*
      - release/*

variables:
  - group: 'Maps-Apps-Pipeline-Secrets'

pool:
  vmImage: 'ubuntu-22.04'

stages:
  - stage: Build
    variables:
      - name: npm_config_cache
        value: $(Pipeline.Workspace)/.npm
    jobs:
      - job: Determine_Affected
        displayName: 'Determine Affected Apps'
        steps:
          - script: |
              cd $(Build.SourcesDirectory)
              echo "the params are build_id: $(Build.BuildId), source_version: $(Build.SourceVersion), tag: dev-$(Build.BuildId)"
            displayName: 'Echo Parameters'

          - checkout: self
            clean: true
            persistCredentials: true
            fetchDepth: 0
            displayName: Check out source code from main repository
          - script: |
              git fetch origin main:refs/remotes/origin/main
            displayName: Fetch the latest history for main branch
          - template: steps/install-node.yml
          - task: Cache@2
            name: npmCache
            displayName: Cache npm shared cache
            inputs:
              key: 'npm | "$(Agent.OS)" | package-lock.json | v2'
              path: $(npm_config_cache)
          - script: npm ci --prefer-offline --no-audit --cache=$(npm_config_cache)
            displayName: 'Install Dependencies'
          - script: npm rebuild nx
            displayName: 'Rebuild NX native binaries'
          - script: |
              chmod +x netlify/scripts/e2e-runner.sh
              ./netlify/scripts/e2e-runner.sh
            displayName: 'Run E2E Runner Script'
            name: e2eRunner

          - script: |
              cd $(Build.SourcesDirectory)

              RUN_ALL_E2E_VALUE="${RUN_ALL_E2E:-false}"

              if [ "$RUN_ALL_E2E_VALUE" = "true" ]; then
                all_apps=$(npx nx show projects --type app)

                echo "Running e2e tests for all apps."

                echo "Formatting output of affected projects..."
                stepoutput=$(echo "$all_apps" | tr '\n' ',')
                stepoutput=${stepoutput%,}

                echo "Setting envstodeploy variable..."
                echo "##vso[task.setvariable variable=envstodeploy;isOutput=true]$stepoutput"
                echo "envstodeploy is set to: $stepoutput"

                exit 0
              fi

              # Fetch the base commit on main
              git fetch origin main --prune
              HEAD_COMMIT=$(git rev-parse HEAD)
              BASE_COMMIT=$(git rev-parse origin/main)

              echo "Base commit: $BASE_COMMIT"
              echo "Head commit: $HEAD_COMMIT"

              # e2e projects are graph type "e2e", not "app": a test-only change affects
              # only the e2e project, so include those to keep e2e jobs running on spec PRs.
              # jq -R + fromjson? extracts the JSON array even when nx writes non-JSON noise
              # to stdout on a cold start; pipefail still surfaces real nx failures
              set -o pipefail
              export NX_DAEMON=false
              echo "Running npx nx show projects --affected for apps and e2e projects"
              affected_apps=$(npx nx show projects --affected --type app --base=$BASE_COMMIT --head=$HEAD_COMMIT --json | jq -R -r 'fromjson? | .[]?')
              affected_e2e=$(npx nx show projects --affected --type e2e --base=$BASE_COMMIT --head=$HEAD_COMMIT --json | jq -R -r 'fromjson? | .[]?')
              affected=$(printf '%s\n%s\n' "$affected_apps" "$affected_e2e" | sed '/^$/d')

              echo "Affected Projects: $affected"

              if [ -z "$affected" ]; then
                echo "No affected projects. Exiting step."
                stepoutput=""
                echo "##vso[task.setvariable variable=envstodeploy;isOutput=true]$stepoutput"
                exit 0
              fi

              echo "Formatting output of affected projects..."
              stepoutput=$(echo "$affected" | tr '\n' ',')
              stepoutput=${stepoutput%,}

              echo "Setting envstodeploy variable..."
              echo "##vso[task.setvariable variable=envstodeploy;isOutput=true]$stepoutput"
              echo "envstodeploy is set to: $stepoutput"
            displayName: 'Determine Affected Projects'
            name: determinenxaffected

  - stage: Code_Analysis
    displayName: 'Code Analysis'
    dependsOn: []

    jobs:
      - job: Analyse_code
        displayName: 'Analyse Code'
        steps:
          - template: steps/analyze-code.yml
            parameters:
              SONARCLOUD_NAME: 'MAPS-SonarCloud'
              SONARCLOUD_ORGANISATION: $(organisationkey)
              SONARCLOUD_PROJECTKEY: $(projectKey)
              SONARCLOUD_PROJECTNAME: 'maps-apps'
              compareWithTargetBranch: false
              keyvaultname: MapsAppsDev
              AKS_SPN_ID: $(AKS_SPN_ID)
              AKS_SPN_KEY: $(aks-spn-key)
              TENANT_ID: $(TENANT_ID)

      - job: Secrets_Scan
        displayName: 'Secrets Scan'
        steps:
          - template: steps/secrets-scan.yml

  - stage: Test
    dependsOn: [Build]
    variables:
      - name: envstodeploy
        value: stageDependencies.Build.Determine_Affected.outputs['determinenxaffected.envstodeploy']

    jobs:
      - template: templates/test-affected.yml
        parameters:
          compareWithTargetBranch: false
          envstodeploy: ${{ variables.envstodeploy }}
          context: deploy-preview
          environment: develop

  - stage: Validate_PR_Title
    displayName: 'Validate PR Title'
    dependsOn: []
    condition: ne(variables['System.PullRequest.PullRequestId'], '')
    pool:
      vmImage: 'ubuntu-22.04'
    jobs:
      - job: Validate_Title
        displayName: 'Validate conventional commit format'
        steps:
          - checkout: self
            clean: true
            fetchDepth: 1
          - script: |
              pip install requests
              python3 netlify/scripts/validate-pr-title.py
            displayName: 'Validate PR title'
            env:
              SYSTEM_ACCESSTOKEN: $(System.AccessToken)
              PR_ID: $(System.PullRequest.PullRequestId)

  - stage: Auto_Publish
    displayName: 'Auto-Publish Draft PR'
    dependsOn: [Build, Code_Analysis, Test, Validate_PR_Title]
    condition: and(succeeded(), ne(variables['System.PullRequest.PullRequestId'], ''))
    pool:
      vmImage: 'ubuntu-22.04'
    jobs:
      - job: Publish_Draft_PR
        displayName: 'Publish Draft PR if tagged'
        steps:
          - checkout: self
            clean: true
            fetchDepth: 1
          - script: |
              pip install requests
              python3 netlify/scripts/auto-publish-pr.py
            displayName: 'Auto-publish draft PR'
            env:
              SYSTEM_ACCESSTOKEN: $(System.AccessToken)
              PR_ID: $(System.PullRequest.PullRequestId)
