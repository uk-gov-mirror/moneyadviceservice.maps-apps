parameters:
  - name: compareWithTargetBranch
    type: boolean
    default: false
  - name: envstodeploy
    type: string
  - name: context
    type: string
  - name: environment
    type: string

jobs:
  - job: test_baby_cost_calculator_if_affected
    displayName: 'baby-cost-calculator'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'baby-cost-calculator'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          testtorun: baby-cost-calculator-e2e
          projectName: 'baby-cost-calculator'
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_baby_money_timeline_if_affected
    displayName: 'baby-money-timeline'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'baby-money-timeline'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          testtorun: baby-money-timeline-e2e
          projectName: 'baby-money-timeline'
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_pensionwise_triage_if_affected
    displayName: 'pensionwise-triage'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'pensionwise-triage'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          testtorun: pensionwise-triage-e2e
          projectName: 'pensionwise-triage'
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_pensionwise_appointment_if_affected
    displayName: 'pensionwise-appointment'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'pensionwise-appointment'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          testtorun: pensionwise-appointment-e2e
          projectName: 'pensionwise-appointment'
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_adjustable_income_calculator_if_affected
    displayName: 'adjustable-income-calculator-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'adjustable-income-calculator'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'adjustable-income-calculator'
          testtorun: adjustable-income-calculator-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_budget_planner_if_affected
    displayName: 'budget-planner-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'budget-planner'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'budget-planner'
          testtorun: budget-planner-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_cash_in_chunks_if_affected
    displayName: 'cash-in-chunks-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'cash-in-chunks'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'cash-in-chunks'
          testtorun: cash-in-chunks-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_learning_pathway_if_affected
    displayName: 'learning-pathway-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'learning-pathway'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'learning-pathway'
          testtorun: learning-pathway-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_moneyhelper_booking_forms_if_affected
    displayName: 'moneyhelper-booking-forms-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'moneyhelper-booking-forms'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'moneyhelper-booking-forms'
          testtorun: moneyhelper-booking-forms-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_moneyhelper_contact_forms_shard_1_if_affected
    displayName: 'moneyhelper-contact-forms-e2e (shard 1/2)'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'moneyhelper-contact-forms'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'moneyhelper-contact-forms'
          testtorun: moneyhelper-contact-forms-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright
          shard: '1/2'

  - job: test_moneyhelper_contact_forms_shard_2_if_affected
    displayName: 'moneyhelper-contact-forms-e2e (shard 2/2)'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'moneyhelper-contact-forms'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'moneyhelper-contact-forms'
          testtorun: moneyhelper-contact-forms-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright
          shard: '2/2'

  - job: test_compare_accounts_if_affected
    displayName: 'compare-accounts-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'compare-accounts'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'compare-accounts'
          testtorun: compare-accounts-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_credit_rejection_if_affected
    displayName: 'credit-rejection-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'credit-rejection'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'credit-rejection'
          testtorun: credit-rejection-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_guaranteed_income_estimator_if_affected
    displayName: 'guaranteed-income-estimator-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'guaranteed-income-estimator'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'guaranteed-income-estimator'
          testtorun: guaranteed-income-estimator-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_leave_pot_untouched_if_affected
    displayName: 'leave-pot-untouched-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'leave-pot-untouched'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'leave-pot-untouched'
          testtorun: leave-pot-untouched-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_moneyhelper_tools_pension_type_if_affected
    displayName: 'tools-pension-type-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'moneyhelper-tools'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'moneyhelper-tools'
          testtorun: moneyhelper-tools-pension-type-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_moneyhelper_tools_workplace_pension_calculator_if_affected
    displayName: 'tools-workplace-pension-calculator-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'moneyhelper-tools'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'moneyhelper-tools'
          testtorun: moneyhelper-tools-workplace-pension-calculator-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_mortgage_calculator_if_affected
    displayName: 'mortgage-calculator-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'mortgage-calculator'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'mortgage-calculator'
          testtorun: mortgage-calculator-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_pensions_dashboard_shard_1_if_affected
    displayName: 'pensions-dashboard (shard 1/5)'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'pensions-dashboard'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'pensions-dashboard'
          testtorun: pensions-dashboard-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright
          shard: '1/5'

  - job: test_pensions_dashboard_shard_2_if_affected
    displayName: 'pensions-dashboard (shard 2/5)'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'pensions-dashboard'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'pensions-dashboard'
          testtorun: pensions-dashboard-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright
          shard: '2/5'

  - job: test_pensions_dashboard_shard_3_if_affected
    displayName: 'pensions-dashboard (shard 3/5)'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'pensions-dashboard'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'pensions-dashboard'
          testtorun: pensions-dashboard-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright
          shard: '3/5'

  - job: test_pensions_dashboard_shard_4_if_affected
    displayName: 'pensions-dashboard (shard 4/5)'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'pensions-dashboard'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'pensions-dashboard'
          testtorun: pensions-dashboard-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright
          shard: '4/5'

  - job: test_pensions_dashboard_shard_5_if_affected
    displayName: 'pensions-dashboard (shard 5/5)'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'pensions-dashboard'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'pensions-dashboard'
          testtorun: pensions-dashboard-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright
          shard: '5/5'

  - job: test_money_adviser_network_if_affected
    displayName: 'money-adviser-network'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'money-adviser-network'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'money-adviser-network'
          testtorun: money-adviser-network-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_savings_calculator_if_affected
    displayName: 'savings-calculator'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'savings-calculator'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'savings-calculator'
          testtorun: savings-calculator-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_standard_financial_statement_if_affected
    displayName: 'standard-financial-statement-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'standard-financial-statement'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'standard-financial-statement'
          testtorun: standard-financial-statement-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_debt_advice_locator_if_affected
    displayName: 'debt-advice-locator-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'debt-advice-locator'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'debt-advice-locator'
          testtorun: debt-advice-locator-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_tools_index_if_affected
    displayName: 'tools-index-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'tools-index'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'tools-index'
          testtorun: tools-index-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_travel_insurance_directory_if_affected
    displayName: 'travel-insurance-directory-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'travel-insurance-directory'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'travel-insurance-directory'
          testtorun: travel-insurance-directory-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_pension_calculator_if_affected
    displayName: 'pension-calculator-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'pension-calculator'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'pension-calculator'
          testtorun: pension-calculator-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_mortgage_affordability_if_affected
    displayName: 'mortgage-affordability-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'mortgage-affordability'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'mortgage-affordability'
          testtorun: mortgage-affordability-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_redundancy_pay_calculator_if_affected
    displayName: 'redundancy-pay-calculator-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'redundancy-pay-calculator'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'redundancy-pay-calculator'
          testtorun: redundancy-pay-calculator-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_retirement_budget_planner_if_affected
    displayName: 'retirement-budget-planner-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'retirement-budget-planner'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'retirement-budget-planner'
          testtorun: retirement-budget-planner-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_retirement_guidance_shard_1_if_affected
    displayName: 'retirement-guidance-e2e (shard 1/2)'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'retirement-guidance'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'retirement-guidance'
          testtorun: retirement-guidance-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright
          shard: '1/2'

  - job: test_retirement_guidance_shard_2_if_affected
    displayName: 'retirement-guidance-e2e (shard 2/2)'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'retirement-guidance'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'retirement-guidance'
          testtorun: retirement-guidance-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright
          shard: '2/2'

  - job: test_take_whole_pot_if_affected
    displayName: 'take-whole-pot-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'take-whole-pot'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'take-whole-pot'
          testtorun: take-whole-pot-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_stamp_duty_calculator_if_affected
    displayName: 'stamp-duty-calculator-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'stamp-duty-calculator'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'stamp-duty-calculator'
          testtorun: stamp-duty-calculator-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_credit_options_if_affected
    displayName: 'credit-options-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'credit-options'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'credit-options'
          testtorun: credit-options-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_evidence_hub_if_affected
    displayName: 'evidence-hub-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'evidence-hub'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'evidence-hub'
          testtorun: evidence-hub-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_fuel_finder_if_affected
    displayName: 'fuel-finder-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'fuel-finder'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'fuel-finder'
          testtorun: fuel-finder-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_midlife_mot_if_affected
    displayName: 'midlife-mot-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'midlife-mot'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'midlife-mot'
          testtorun: midlife-mot-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: cypress

  - job: test_salary_calculator_if_affected
    displayName: 'salary-calculator-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'salary-calculator'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'salary-calculator'
          testtorun: salary-calculator-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_pension_type_tool_if_affected
    displayName: 'pension-type-tool-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'pension-type-tool'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'pension-type-tool'
          testtorun: pension-type-tool-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: test_workplace_pension_calculator_if_affected
    displayName: 'workplace-pension-calculator-e2e'
    condition: and(succeeded(), contains(${{ parameters.envstodeploy }}, 'workplace-pension-calculator'))
    steps:
      - template: ../steps/run-tests.yml
        parameters:
          compareWithTargetBranch: ${{ parameters.compareWithTargetBranch }}
          projectName: 'workplace-pension-calculator'
          testtorun: workplace-pension-calculator-e2e
          context: ${{ parameters.context }}
          environment: ${{ parameters.environment }}
          testrunner: playwright

  - job: post_collated_playwright_reports
    displayName: 'Post Collated Playwright Reports to PR'
    dependsOn:
      - test_baby_cost_calculator_if_affected
      - test_baby_money_timeline_if_affected
      - test_pensionwise_triage_if_affected
      - test_pensionwise_appointment_if_affected
      - test_adjustable_income_calculator_if_affected
      - test_budget_planner_if_affected
      - test_cash_in_chunks_if_affected
      - test_moneyhelper_booking_forms_if_affected
      - test_moneyhelper_contact_forms_shard_1_if_affected
      - test_moneyhelper_contact_forms_shard_2_if_affected
      - test_compare_accounts_if_affected
      - test_credit_rejection_if_affected
      - test_guaranteed_income_estimator_if_affected
      - test_learning_pathway_if_affected
      - test_leave_pot_untouched_if_affected
      - test_moneyhelper_tools_pension_type_if_affected
      - test_moneyhelper_tools_workplace_pension_calculator_if_affected
      - test_mortgage_calculator_if_affected
      - test_pensions_dashboard_shard_1_if_affected
      - test_pensions_dashboard_shard_2_if_affected
      - test_pensions_dashboard_shard_3_if_affected
      - test_pensions_dashboard_shard_4_if_affected
      - test_pensions_dashboard_shard_5_if_affected
      - test_money_adviser_network_if_affected
      - test_savings_calculator_if_affected
      - test_standard_financial_statement_if_affected
      - test_debt_advice_locator_if_affected
      - test_tools_index_if_affected
      - test_travel_insurance_directory_if_affected
      - test_mortgage_affordability_if_affected
      - test_redundancy_pay_calculator_if_affected
      - test_retirement_budget_planner_if_affected
      - test_retirement_guidance_shard_1_if_affected
      - test_retirement_guidance_shard_2_if_affected
      - test_take_whole_pot_if_affected
      - test_stamp_duty_calculator_if_affected
      - test_credit_options_if_affected
      - test_evidence_hub_if_affected
      - test_fuel_finder_if_affected
      - test_midlife_mot_if_affected
      - test_salary_calculator_if_affected
      - test_pension_type_tool_if_affected
      - test_workplace_pension_calculator_if_affected
      - test_pension_calculator_if_affected
    condition: and(always(), ne(variables['System.PullRequest.PullRequestId'], ''))
    steps:
      - checkout: none

      - task: AzureCLI@2
        displayName: 'Collect and Post Playwright Reports'
        inputs:
          azureSubscription: 'ADO-PWD-Connection'
          scriptType: 'bash'
          scriptLocation: 'inlineScript'
          inlineScript: |
            STORAGE_ACCOUNT="mapsplaywrightreports"
            ACCOUNT_KEY=$(az storage account keys list --account-name "$STORAGE_ACCOUNT" --query "[0].value" --output tsv)
            METADATA_CONTAINER="playwright-report-metadata"
            PR_ID="$(System.PullRequest.PullRequestId)"
            METADATA_PREFIX="pr-${PR_ID}/"

            echo "Collecting Playwright report metadata for PR $PR_ID..."

            # List all metadata files for this PR
            METADATA_FILES=$(az storage blob list \
              --account-name "$STORAGE_ACCOUNT" \
              --account-key "$ACCOUNT_KEY" \
              --container-name "$METADATA_CONTAINER" \
              --prefix "$METADATA_PREFIX" \
              --query "[].name" \
              --output tsv \
              --auth-mode key)

            if [ -z "$METADATA_FILES" ]; then
              echo "No Playwright reports found for this PR."
              exit 0
            fi

            echo "Found metadata files:"
            echo "$METADATA_FILES"

            # Build HTML comment with all reports
            COMMENT_BODY="<h2>🎭 Playwright Test Reports</h2>"
            COMMENT_BODY="${COMMENT_BODY}<p>The following Playwright test reports are available:</p>"
            COMMENT_BODY="${COMMENT_BODY}<ul>"

            # Download and parse each metadata file
            while IFS= read -r metadata_file; do
              if [ -n "$metadata_file" ]; then
                echo "Processing: $metadata_file"

                # Download metadata to temp file
                TEMP_FILE=$(mktemp)
                az storage blob download \
                  --account-name "$STORAGE_ACCOUNT" \
                  --account-key "$ACCOUNT_KEY" \
                  --container-name "$METADATA_CONTAINER" \
                  --name "$metadata_file" \
                  --file "$TEMP_FILE" \
                  --auth-mode key \
                  --no-progress > /dev/null 2>&1

                # Read metadata
                METADATA_JSON=$(cat "$TEMP_FILE")
                rm -f "$TEMP_FILE"

                # Extract fields
                PROJECT_NAME=$(echo "$METADATA_JSON" | jq -r '.projectName')
                TEST_NAME=$(echo "$METADATA_JSON" | jq -r '.testName')
                SHARD=$(echo "$METADATA_JSON" | jq -r '.shard')
                REPORT_URL=$(echo "$METADATA_JSON" | jq -r '.reportUrl')

                # Format display name
                if [ -n "$SHARD" ] && [ "$SHARD" != "null" ]; then
                  DISPLAY_NAME="${TEST_NAME} (shard ${SHARD})"
                else
                  DISPLAY_NAME="${TEST_NAME}"
                fi

                # Add to comment
                COMMENT_BODY="${COMMENT_BODY}<li><strong>${DISPLAY_NAME}</strong>: <a href='${REPORT_URL}' target='_blank'>View Report</a></li>"
              fi
            done <<< "$METADATA_FILES"

            COMMENT_BODY="${COMMENT_BODY}</ul>"
            COMMENT_BODY="${COMMENT_BODY}<p><em>Reports expire after 7 days</em></p>"

            echo "Constructed comment body:"
            echo "$COMMENT_BODY"

            # Check if an existing Playwright reports comment exists
            ADO_API_BASE="https://dev.azure.com/moneyandpensionsservice/MaPS%20Digital/_apis/git/repositories/$(Build.Repository.Name)"
            THREADS_API="${ADO_API_BASE}/pullrequests/${PR_ID}/threads?api-version=7.1-preview.1"

            echo "Checking for existing Playwright reports comment..."
            EXISTING_THREADS=$(curl -s -X GET "$THREADS_API" \
              --header "Authorization: Bearer $SYSTEM_ACCESSTOKEN")

            # Search for thread containing our unique identifier
            EXISTING_THREAD_ID=$(echo "$EXISTING_THREADS" | jq -r '.value[] | select(.comments[0].content | contains("🎭 Playwright Test Reports")) | .id' | head -1)

            if [ -n "$EXISTING_THREAD_ID" ] && [ "$EXISTING_THREAD_ID" != "null" ]; then
              echo "Found existing thread ID: $EXISTING_THREAD_ID - updating..."

              # Get the first comment ID in the thread
              EXISTING_COMMENT_ID=$(echo "$EXISTING_THREADS" | jq -r ".value[] | select(.id == $EXISTING_THREAD_ID) | .comments[0].id")

              # Update existing comment
              UPDATE_API="${ADO_API_BASE}/pullrequests/${PR_ID}/threads/${EXISTING_THREAD_ID}/comments/${EXISTING_COMMENT_ID}?api-version=7.1-preview.1"

              UPDATE_PAYLOAD=$(jq -n --arg content "$COMMENT_BODY" '{content: $content}')

              curl -X PATCH "$UPDATE_API" \
                --header "Content-Type: application/json" \
                --header "Accept: application/json" \
                --header "Authorization: Bearer $SYSTEM_ACCESSTOKEN" \
                --data "$UPDATE_PAYLOAD" \
                --verbose

              echo "Existing Playwright reports comment updated successfully!"
            else
              echo "No existing thread found - creating new comment..."

              # Create new comment
              PR_COMMENT=$(jq -n --arg comment "$COMMENT_BODY" '{"comments": [{"parentCommentId": 0, "content": $comment, "commentType": 1}], "status": "fixed"}')

              curl -X POST "$THREADS_API" \
                --header "Content-Type: application/json" \
                --header "Accept: application/json" \
                --header "Authorization: Bearer $SYSTEM_ACCESSTOKEN" \
                --data "$PR_COMMENT" \
                --verbose

              echo "New Playwright reports comment posted successfully!"
            fi
        env:
          SYSTEM_ACCESSTOKEN: $(System.AccessToken)
